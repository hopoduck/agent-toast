{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all windows",
  "windows": ["main", "notify-*", "setup"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-set-focus",
    "core:window:allow-close",
    "updater:default",
    "process:allow-restart"
  ]
}
